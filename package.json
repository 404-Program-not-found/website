{
  "private": true,
  "scripts": {
    "build": "nuxt build TAILWIND_MODE=watch",
    "dev": "nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "tailwind-config-viewer": "tailwind-config-viewer --open --port 3001"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "nuxt": "3.0.0",
    "postcss": "^8.4.20",
    "tailwind-config-viewer": "^1.7.2",
    "tailwindcss": "^3.2.4"
  },
  "dependencies": {
    "@vueuse/motion": "2.0.0-beta.25",
    "flowbite": "^1.5.5",
    "uuid": "^9.0.0"
  }
}
